# ------------------------------------------------------
# Local development environment configuration
# ------------------------------------------------------
# Ensures consistent Node.js version and project context
# across local environments using direnv.
# ------------------------------------------------------

# Load the Node.js version defined in .nvmrc.
# Requires direnv with node layout support enabled.
layout node
